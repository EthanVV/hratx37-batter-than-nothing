const donuts = require('./donuts');
const donut_bases = require('./donut_bases');
const donut_types = require('./donut_types');
const toppings = require('./toppings');
const orders = require('./orders');

module.exports = {
  donuts,
  donut_bases,
  donut_types,
  toppings,
  orders
};